import apiControllers from '@src/controllers/api';
import logicControllers from '@src/controllers/logic';

const controllers = {
    apiControllers: apiControllers,
    logicControllers: logicControllers,

}

export default {
    apiControllers: apiControllers,
    logicControllers: logicControllers,

};